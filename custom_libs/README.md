# Custom Libraries

Modified version of [obs-websocket-dotnet](https://github.com/BarRaider/obs-websocket-dotnet) library are located here.

`obs-websocket-dotnet.dll` is used by the OBS Notifier application itself.
